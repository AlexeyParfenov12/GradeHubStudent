<?php

// Absolute filesystem path to the project root
if (!defined('BASE_PATH')) {
    define('BASE_PATH', __DIR__);
}

// Base URL prefix for building absolute links.
// If the project is served at domain root, keep '/'.
// If it's under a subfolder, e.g. http://host/GradeHub4, set '/GradeHub4'.
if (!defined('BASE_URL')) {
    define('BASE_URL', '/');
}




